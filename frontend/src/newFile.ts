import { urlParams } from './App.vue';

if (urlParams.get('payment_status')) {
if (urlParams.get('payment_status') === 'completed' && urlParams.get('payment_status') === 'succeeded') {
api;
} else {
// failed
}
}
